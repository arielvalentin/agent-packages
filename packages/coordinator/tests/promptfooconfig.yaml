description: "Coordinator package behavioral tests"

providers:
  - id: ollama:llama3.2:3b
    config:
      temperature: 0
    label: skill-under-test

prompts:
  - "{{skill_content}}\n\nYou are an AI agent following the skill instructions above. Respond to the user's request.\n\nUser: {{user_input}}"

tests:
  # --- tech-research ---
  - description: "tech-research: runs same-source queries sequentially"
    vars:
      skill_content: file://../.apm/skills/tech-research/SKILL.md
      user_input: "I have three related questions that all require searching GitHub. Should I launch three researchers in parallel? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "tech-research: allows parallel distinct backends"
    vars:
      skill_content: file://../.apm/skills/tech-research/SKILL.md
      user_input: "Can I research GitHub, Azure documentation, and the public web at the same time? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^yes[.!]?$/.test(l)"

  - description: "tech-research: requires evidence and one synthesis"
    vars:
      skill_content: file://../.apm/skills/tech-research/SKILL.md
      user_input: "What should each researcher return, and who performs the single synthesis?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('source') || l.includes('citation') || l.includes('url')) && l.includes('fact') && l.includes('coordinator') && (l.includes('single') || l.includes('once')) && l.includes('synthes') && !l.includes('each researcher synthes')"

  - description: "tech-research: never duplicates research for consensus"
    vars:
      skill_content: file://../.apm/skills/tech-research/SKILL.md
      user_input: "Should I ask multiple models to research the same Azure documentation pages so I can build consensus? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "tech-research: routes short answers inline"
    vars:
      skill_content: file://../.apm/skills/tech-research/SKILL.md
      user_input: "Where should a short factual answer with no durable artifact go? Answer exactly with the destination."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('inline') && !l.includes('system-architect') && !l.includes('se-technical-writer')"

  - description: "tech-research: routes architecture input"
    vars:
      skill_content: file://../.apm/skills/tech-research/SKILL.md
      user_input: "Where should research for a design or architectural decision go? Answer exactly with the destination."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('system-architect') && !l.includes('se-technical-writer')"

  - description: "tech-research: routes technical reports"
    vars:
      skill_content: file://../.apm/skills/tech-research/SKILL.md
      user_input: "Where should synthesized findings for a technical report go? Answer exactly with the destination."
    assert:
      - type: javascript
        value: "output.toLowerCase().includes('se-technical-writer')"

  - description: "tech-research: hands off durable synthesis without re-querying"
    vars:
      skill_content: file://../.apm/skills/tech-research/SKILL.md
      user_input: "How do I hand durable synthesized research to another agent, and should that agent repeat the covered backend queries?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('artifact_paths') && /(do not|don't|should not|must not|never|avoid|without|no need|not necessary|skip)/.test(l) && /(re-?query|repeat|redundant|duplicate)/.test(l) && (l.includes('covered') || l.includes('backend'))"

  # --- pr-lifecycle ---
  - description: "pr-lifecycle: draft PR create command is non-interactive — a conventional --title plus a --body in position (no prose fallback, no <type> placeholder exception), and a later gh pr edit --title/--body cannot launder a WIP/missing create title"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "I am fixing a null-pointer crash in the login handler on this bugfix branch, and no draft PR exists yet. What exact `gh pr create` command do I run right now?"
    assert:
      - type: icontains
        value: "--draft"
      - type: icontains
        value: "gh pr create"
      - type: icontains
        value: "--body"
      - type: javascript
        value: "const raw = output; const BT = String.fromCharCode(96); const DQ = String.fromCharCode(34); const SQ = String.fromCharCode(39); const SENT = String.fromCharCode(1); const normalized = raw.replace(/\\\\\\s*\\n\\s*/g, ' '); function ciIndexOf(text, needle, from) { const escaped = needle.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&').replace(/\\s+/g, '\\\\s+'); const re = new RegExp(escaped, 'i'); const rel = text.slice(from || 0).search(re); return rel === -1 ? -1 : rel + (from || 0); } function maskFlag(text, flag) { return text.replace(new RegExp('(--' + flag + '\\\\s*=?\\\\s*)' + BT + '([^' + BT + ']*)' + BT, 'gi'), function (m, p1, p2) { return p1 + SENT + p2 + SENT; }); } const masked = maskFlag(maskFlag(normalized, 'title'), 'body'); function extractCreateSegments(text) { const out = []; const anchor = 'gh pr create'; const ghRe = /\\bgh\\b/gi; let idx = ciIndexOf(text, anchor); while (idx !== -1) { const searchFrom = idx + anchor.length; const nextCreate = ciIndexOf(text, anchor, searchFrom); const nextBacktick = text.indexOf(BT, searchFrom); const nextNewline = text.indexOf('\\n', searchFrom); ghRe.lastIndex = searchFrom; const ghMatch = ghRe.exec(text); const nextGh = ghMatch ? ghMatch.index : -1; const stops = [nextCreate, nextBacktick, nextNewline, nextGh].filter((i) => i !== -1); const end = stops.length ? Math.min(...stops) : text.length; out.push(text.slice(idx, end)); idx = nextCreate; } return out; } const createSegments = extractCreateSegments(masked).map((seg) => seg.split(/\\s*(?:&&|;|\\|\\|)\\s*/)[0]); function extractArg(segment, flag) { const anchorRe = new RegExp('--' + flag + '\\\\s*=?\\\\s*', 'i'); const m = anchorRe.exec(segment); if (!m) return null; const rest = segment.slice(m.index + m[0].length); let val = ''; if (rest[0] === DQ) { const end = rest.indexOf(DQ, 1); val = end === -1 ? rest.slice(1) : rest.slice(1, end); } else if (rest[0] === SQ) { const end = rest.indexOf(SQ, 1); val = end === -1 ? rest.slice(1) : rest.slice(1, end); } else if (rest[0] === SENT) { const end = rest.indexOf(SENT, 1); val = end === -1 ? rest.slice(1) : rest.slice(1, end); } else { const flagMatch = rest.match(/\\s+--[a-z]/i); const flagIdx = flagMatch ? flagMatch.index : -1; const nlIdx = rest.indexOf('\\n'); const stops = [flagIdx, nlIdx].filter((i) => i !== -1); const endIdx = stops.length ? Math.min(...stops) : rest.length; val = rest.slice(0, endIdx); } return val.trim(); } const conventionalRe = /^(feat|fix|docs|refactor|test|chore|ci|perf|build|revert)(\\([^()\\s]+\\))?!?:\\s+\\S/; const wipRe = /^wip\\b/i; const draftCreateSegments = createSegments.filter((segment) => /--draft\\b/i.test(segment)); const hasDraftCreate = draftCreateSegments.length > 0; const allDraftCreatesValid = hasDraftCreate && draftCreateSegments.every((segment) => { const title = extractArg(segment, 'title'); const body = extractArg(segment, 'body'); return title !== null && title !== '' && conventionalRe.test(title) && !wipRe.test(title) && body !== null && body !== ''; }); hasDraftCreate && allDraftCreatesValid"

  - description: "pr-lifecycle: flow-to-type mapping is a default, not absolute"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "I'm in a bugfix flow, but the very first change is documentation-only. Reply with exactly two lines and nothing else, in this exact format:\nOverridable: <Yes or No> (can the flow's default type mapping be overridden when the actual initial change is docs, test, chore, ci, perf, build, or revert work?)\nType: <the single Conventional Commits type token you would use for the draft PR title in this situation>"
    assert:
      - type: javascript
        value: "const BT = String.fromCharCode(96); const stripped = output.replace(new RegExp(BT, 'g'), ''); const overridableMatch = /overridable\\s*:\\s*(yes|no)\\b/i.exec(stripped); const typeMatch = /type\\s*:\\s*([a-z]+)\\b/i.exec(stripped); const isOverridable = !!overridableMatch && overridableMatch[1].toLowerCase() === 'yes'; const typeIsDocs = !!typeMatch && typeMatch[1].toLowerCase() === 'docs'; isOverridable && typeIsDocs"

  - description: "pr-lifecycle: rejects an empty scope as invalid grammar (explicit leading rejection)"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "Is `fix(): correct the bug` a valid PR title under this skill's title format? Start your reply with exactly one word, Yes or No, then state the specific reason."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase().replace(/[*_#]/g, '').trim(); const BT = String.fromCharCode(96); const stripped = l.split(BT).join(''); const startsNo = /^no\\b/.test(stripped); const mentionsEmptyScope = /\\(\\)/.test(stripped) || (/\\bscope\\b/.test(stripped) && /\\b(empty|blank|missing|nothing|no\\s+scope)\\b/.test(stripped)); startsNo && mentionsEmptyScope"

  - description: "pr-lifecycle: rejects a whitespace-only scope as invalid grammar"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "Is `fix( ): correct the bug` (a scope made only of a single space) a valid PR title under this skill's title format? Start your reply with exactly one word, Yes or No, then state the specific reason."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase().replace(/[*_#]/g, '').trim(); const BT = String.fromCharCode(96); const stripped = l.split(BT).join(''); const startsNo = /^no\\b/.test(stripped); const mentionsWhitespaceScope = /\\bscope\\b/.test(stripped) && /\\b(empty|blank|missing|nothing|whitespace|no\\s+scope|only\\s+(a\\s+)?spaces?|only\\s+whitespace|not\\s+(a\\s+)?(valid|non-empty|non-whitespace)|invalid)\\b/.test(stripped); startsNo && mentionsWhitespaceScope"

  - description: "pr-lifecycle: rejects a whitespace-only description as invalid grammar"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "Is `fix:   ` (a colon followed only by spaces, with no description text) a valid PR title under this skill's title format? Start your reply with exactly one word, Yes or No, then state the specific reason."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase().replace(/[*_#]/g, '').trim(); const BT = String.fromCharCode(96); const stripped = l.split(BT).join(''); const startsNo = /^no\\b/.test(stripped); const mentionsEmptyDescription = /\\bdescription\\b/.test(stripped) && /\\b(empty|blank|missing|nothing|whitespace|no\\s+description|only\\s+spaces|only\\s+whitespace)\\b/.test(stripped); startsNo && mentionsEmptyDescription"

  - description: "pr-lifecycle: title is unscoped by default"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "Under this skill's Title format, is a (scope) required in the PR title by default? Start your reply with exactly one word, Yes or No."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase().replace(/[*_#]/g, '').trim(); const BT = String.fromCharCode(96); const stripped = l.split(BT).join(''); /^no\\b/.test(stripped)"

  - description: "pr-lifecycle: scope is permitted only when material"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "Under this skill's Title format, is the scoped form <type>(<scope>): <description> allowed only when the scope materially clarifies the change, rather than being usable at any time for any reason? Start your reply with exactly one word, Yes or No."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase().replace(/[*_#]/g, '').trim(); const BT = String.fromCharCode(96); const stripped = l.split(BT).join(''); /^yes\\b/.test(stripped)"

  - description: "pr-lifecycle: breaking changes use the optional ! marker"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "According to the Title format section, exactly how do I write a breaking-change PR title? Quote the format."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); const grammarRe = /(<type>|feat|fix|docs|refactor|test|chore|ci|perf|build|revert)(\\(<scope>\\)|\\([^()\\s]+\\))?!:\\s+\\S/; grammarRe.test(l) && l.includes('break')"

  - description: "pr-lifecycle: validates title before create"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "Before running `gh pr create` to open a PR, must I validate the title against the Title format section? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^yes[.!]?$/.test(l)"

  - description: "pr-lifecycle: validates title before update"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "Before running `gh pr edit --title` to update a PR's title, must I validate the new title against the Title format section? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^yes[.!]?$/.test(l)"

  - description: "pr-lifecycle: validates title before ready"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "Before running `gh pr ready` to mark a PR ready for review, must I validate the title against the Title format section? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^yes[.!]?$/.test(l)"

  - description: "pr-lifecycle: finalization re-derives/corrects the title and explicitly rejects rename/convert-to-conventional framing"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "My PR is ready to finalize. Statement: finalization converts or renames the PR title into Conventional Commits format for the first time. Is that statement true or false? Start your reply with exactly one word, True or False, then explain what finalization actually does to the title."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); const stripped = l.replace(/[*_#]/g, '').split(String.fromCharCode(96)).join('').trim(); const leadingDecline = /^false\\b/.test(stripped); const describesCorrection = /\\b(re-derive[sd]?|rederive[sd]?|correct(?:s|ed|ion)?|refine[sd]?|refinement)\\b/.test(l); leadingDecline && describesCorrection"

  - description: "pr-lifecycle: skips early draft PR for research flows"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "I am doing a research flow to investigate a performance issue. Should I open a PR?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('skip') || l.includes('no') || l.includes('not') || l.includes(\"don't\") || l.includes('unnecessary')"

  - description: "pr-lifecycle: monitors CI after push"
    vars:
      skill_content: file://../.apm/skills/pr-lifecycle/SKILL.md
      user_input: "I just pushed commits to my PR branch. What should I do next?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('gh pr checks') || l.includes('gh pr create') || l.includes('ci') || l.includes('check') || l.includes('review') || l.includes('monitor')"

  # --- pr-review-protocol ---
  - description: "pr-review-protocol: waits for required CI before review"
    vars:
      skill_content: file://../.apm/skills/pr-review-protocol/SKILL.md
      user_input: "The pull request has a failing required check. Should I review it now?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('stop') || l.includes('wait') || l.includes('not proceed') || l.includes(\"don't review\") || l.includes('do not review')) && (l.includes('green') || l.includes('pass') || l.includes('required check'))"

  - description: "pr-review-protocol: establishes intent before reviewing code"
    vars:
      skill_content: file://../.apm/skills/pr-review-protocol/SKILL.md
      user_input: "What context should I gather before reviewing a pull request diff?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('description') || l.includes('issue') || l.includes('intent') || l.includes('acceptance') || l.includes('rationale')"

  - description: "pr-review-protocol: targets the repository and reuses context"
    vars:
      skill_content: file://../.apm/skills/pr-review-protocol/SKILL.md
      user_input: "How do I avoid reviewing the wrong repository or repeatedly fetching the same PR?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('--repo') || l.includes('owner/repo') || l.includes('repository')) && (l.includes('once') || l.includes('artifact') || l.includes('reuse') || l.includes('saved'))"

  - description: "pr-review-protocol: requires evidence and one synthesized review"
    vars:
      skill_content: file://../.apm/skills/pr-review-protocol/SKILL.md
      user_input: "Should I post several quick review comments based only on my opinions?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('evidence') || l.includes('documentation') || l.includes('spec') || l.includes('repository')) && (l.includes('one') || l.includes('single') || l.includes('synthes'))"

  - description: "adversarial-review: uses independently dispatched reviewers"
    vars:
      skill_content: file://../.apm/skills/adversarial-review/SKILL.md
      user_input: "How does the adversarial review work? What models are used?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('independent') || l.includes('parallel')) && (l.includes('gpt-first') || l.includes('consensus-panel') || l.includes('consensus panel'))"

  - description: "adversarial-review: standalone selection delegates to canonical GPT-first policy"
    vars:
      skill_content: file://../.apm/skills/adversarial-review/SKILL.md
      user_input: "For a standalone substantive review, the post-reservation Wave 1 preferred-tier pool contains three suitable IDs in catalog order: gpt-5.5, claude-sonnet-5, and gpt-5.4. Both GPT IDs remain available. Which Wave 1 pair follows the canonical GPT-first policy? A: gpt-5.5 and gpt-5.4. B: gpt-5.5 and claude-sonnet-5. Answer exactly one letter: A or B."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^a\\b/.test(l) || /\\b(?:answer|choice|pair)\\s+(?:is\\s+)?a\\b/.test(l)"

  - description: "adversarial-review: family and tier preferences cannot override GPT-first selection"
    vars:
      skill_content: file://../.apm/skills/adversarial-review/SKILL.md
      user_input: "May a standalone adversarial review replace an eligible GPT Wave 1 candidate with a different-family model solely to increase family diversity? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "adversarial-review: panel members do not spawn nested reviews"
    vars:
      skill_content: file://../.apm/skills/adversarial-review/SKILL.md
      user_input: "My handoff says consensus_role: panel-member. Should I dispatch more reviewer agents?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('do not') || l.includes(\"don't\") || l.includes('no') || l.includes('not dispatch')) && (l.includes('direct') || l.includes('single') || l.includes('panel'))"

  # --- review-fix-loop ---
  - description: "review-fix-loop: escalates after max retries"
    vars:
      skill_content: file://../.apm/skills/review-fix-loop/SKILL.md
      user_input: "What happens if the review-fix loop keeps finding issues after many retries?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('escalat') || l.includes('stop') || l.includes('max') || l.includes('limit') || l.includes('abort') || l.includes('exhaust')"

  - description: "review-fix-loop: specialist reviews use consensus panel"
    vars:
      skill_content: file://../.apm/skills/review-fix-loop/SKILL.md
      user_input: "How should the reviewer be dispatched during a review-fix loop?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('consensus-panel') || l.includes('consensus panel') || l.includes('panel policy')"

  # --- acting-on-behalf ---
  - description: "acting-on-behalf: includes AI disclaimer in PR comments"
    vars:
      skill_content: file://../.apm/skills/acting-on-behalf/SKILL.md
      user_input: "I need to post a comment on a PR. What must I include?"
    assert:
      - type: icontains
        value: "disclaimer"
      - type: icontains
        value: "AI"

  - description: "acting-on-behalf: requires adversarial review before PR creation"
    vars:
      skill_content: file://../.apm/skills/acting-on-behalf/SKILL.md
      user_input: "What safety checks do I need before opening a PR with code changes?"
    assert:
      - type: icontains
        value: "adversarial"

  - description: "acting-on-behalf: pr-lifecycle-missing fallback requires a case-sensitive valid non-WIP --title plus --body on the same gh pr create --draft command (a bare create followed by a later gh pr edit --title/--body cannot launder it, and an uppercase FIX: title must fail)"
    vars:
      skill_content: file://../.apm/skills/acting-on-behalf/SKILL.md
      user_input: "The pr-lifecycle skill is unavailable and no draft PR exists yet for my bugfix branch. What exact `gh pr create` command do I run right now?"
    assert:
      - type: icontains
        value: "--draft"
      - type: icontains
        value: "--title"
      - type: icontains
        value: "--body"
      - type: javascript
        value: "const raw = output; const BT = String.fromCharCode(96); const DQ = String.fromCharCode(34); const SQ = String.fromCharCode(39); const SENT = String.fromCharCode(1); const normalized = raw.replace(/\\\\\\s*\\n\\s*/g, ' '); function ciIndexOf(text, needle, from) { const escaped = needle.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&').replace(/\\s+/g, '\\\\s+'); const re = new RegExp(escaped, 'i'); const rel = text.slice(from || 0).search(re); return rel === -1 ? -1 : rel + (from || 0); } function maskFlag(text, flag) { return text.replace(new RegExp('(--' + flag + '\\\\s*=?\\\\s*)' + BT + '([^' + BT + ']*)' + BT, 'gi'), function (m, p1, p2) { return p1 + SENT + p2 + SENT; }); } const masked = maskFlag(maskFlag(normalized, 'title'), 'body'); function extractCreateSegments(text) { const out = []; const anchor = 'gh pr create'; const ghRe = /\\bgh\\b/gi; let idx = ciIndexOf(text, anchor); while (idx !== -1) { const searchFrom = idx + anchor.length; const nextCreate = ciIndexOf(text, anchor, searchFrom); const nextBacktick = text.indexOf(BT, searchFrom); const nextNewline = text.indexOf('\\n', searchFrom); ghRe.lastIndex = searchFrom; const ghMatch = ghRe.exec(text); const nextGh = ghMatch ? ghMatch.index : -1; const stops = [nextCreate, nextBacktick, nextNewline, nextGh].filter((i) => i !== -1); const end = stops.length ? Math.min(...stops) : text.length; out.push(text.slice(idx, end)); idx = nextCreate; } return out; } const createSegments = extractCreateSegments(masked).map((seg) => seg.split(/\\s*(?:&&|;|\\|\\|)\\s*/)[0]); function extractArg(segment, flag) { const anchorRe = new RegExp('--' + flag + '\\\\s*=?\\\\s*', 'i'); const m = anchorRe.exec(segment); if (!m) return null; const rest = segment.slice(m.index + m[0].length); let val = ''; if (rest[0] === DQ) { const end = rest.indexOf(DQ, 1); val = end === -1 ? rest.slice(1) : rest.slice(1, end); } else if (rest[0] === SQ) { const end = rest.indexOf(SQ, 1); val = end === -1 ? rest.slice(1) : rest.slice(1, end); } else if (rest[0] === SENT) { const end = rest.indexOf(SENT, 1); val = end === -1 ? rest.slice(1) : rest.slice(1, end); } else { const flagMatch = rest.match(/\\s+--[a-z]/i); const flagIdx = flagMatch ? flagMatch.index : -1; const nlIdx = rest.indexOf('\\n'); const stops = [flagIdx, nlIdx].filter((i) => i !== -1); const endIdx = stops.length ? Math.min(...stops) : rest.length; val = rest.slice(0, endIdx); } return val.trim(); } const conventionalRe = /^(feat|fix|docs|refactor|test|chore|ci|perf|build|revert)(\\([^()\\s]+\\))?!?:\\s+\\S/; const wipRe = /^wip\\b/i; const draftCreateSegments = createSegments.filter((segment) => /--draft\\b/i.test(segment)); const hasDraftCreate = draftCreateSegments.length > 0; const allDraftCreatesValid = hasDraftCreate && draftCreateSegments.every((segment) => { const title = extractArg(segment, 'title'); const body = extractArg(segment, 'body'); return title !== null && title !== '' && conventionalRe.test(title) && !wipRe.test(title) && body !== null && body !== ''; }); hasDraftCreate && allDraftCreatesValid"

  # --- consensus-panel ---
  - description: "consensus-panel: dispatches to multiple models in parallel"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "How does the consensus panel work?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('parallel') || l.includes('multiple') || l.includes('dispatch')) && (l.includes('consensus') || l.includes('verdict') || l.includes('synthesiz') || l.includes('unified'))"

  - description: "consensus-panel: initial wave is exactly two reviewers"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "How many reviewers do I dispatch in the initial wave? Answer with exactly one number."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#.]/g, '').split(String.fromCharCode(96)).join(''); /(^|\\W)(2|two)(\\W|$)/.test(l) && !/(^|\\W)(3|three)(\\W|$)/.test(l)"

  - description: "consensus-panel: GPT classification follows the normalized model ID"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "According to Step 2b, complete this sentence exactly: A candidate is GPT when..."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('normalized model id') && l.includes('gpt-')"

  - description: "consensus-panel: family metadata cannot promote a non-GPT ID"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "According to Step 2b, may family metadata override the normalized model-ID rule? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: duplicate model IDs use the first occurrence"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "When the runtime catalog repeats an identical model ID, which occurrence wins and which selection metadata is retained?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('first occurrence') && l.includes('wins')"

  - description: "consensus-panel: uses runtime-discovery capability categories"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Does panel selection require a high_capability boolean field, or does it use the existing runtime model-discovery capability categorization?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('does not require') || l.includes('without requiring') || l.includes('no')) && l.includes('runtime model-discovery capability categorization')"

  - description: "consensus-panel: reserves an unused high-capability GPT"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "The suitable catalog, in order, is gpt-high (runtime category: high-capability), gpt-review-a (runtime category: mid-tier), and gpt-review-b (runtime category: fast-capable). Which single ID does Step 2b reserve for Wave 2? Answer with only the model ID."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('gpt-high') && !l.includes('gpt-review-a') && !l.includes('gpt-review-b')"

  - description: "consensus-panel: Wave 1 prefers review-tier GPT candidates"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Name only the first capability/family class in Step 2b's ordered Wave 1 selection."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('mid-tier') && !l.includes('non-gpt') && !l.includes('high-capability')"

  - description: "consensus-panel: Wave 1 excludes fast-light models"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Only one mid-tier GPT remains after reservation, plus one fast/light GPT and one mid-tier non-GPT. Which candidate is ineligible for Wave 1?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); const fastLight = l.includes('fast/light') || l.includes('fast-light'); fastLight && (l.includes('ineligible') || l.includes('exclude'))"

  - description: "consensus-panel: high-capability GPT is an exhaustion fallback"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "After reserving one high-capability GPT, only one mid-tier GPT remains, plus another high-capability GPT and a mid-tier non-GPT. Which capability/family class fills the second Wave 1 slot?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('high-capability gpt') && (l.includes('second') || l.includes('fill'))"

  - description: "consensus-panel: non-GPT fills only a GPT shortfall"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "According to Step 2b, when may a suitable non-GPT candidate fill a missing Wave 1 slot?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); const eligible = l.includes('wave 1-eligible gpt') || l.includes('non-fast-light gpt') || l.includes('non-fast/light gpt'); l.includes('insufficient') && eligible"

  - description: "consensus-panel: fast-light GPT does not block non-GPT fallback"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Apply Step 2b's Wave 1 quality floor before measuring GPT sufficiency. The complete suitable catalog is gpt-mid (runtime category: mid-tier), gpt-light (runtime category: fast/light), and claude-mid (runtime category: mid-tier). No Wave 2 reservation is available. Which Wave 1 pair follows the policy? A: gpt-mid and claude-mid. B: gpt-mid and gpt-light. Answer exactly one letter: A or B."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^a[.!]?$/.test(l)"

  - description: "consensus-panel: ordered catalog fills GPT shortfall with non-GPT"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "The complete suitable ordered catalog is claude-review-a (runtime category: mid-tier), then gpt-review-a (runtime category: fast-capable). No other suitable IDs exist. Which two model IDs fill Wave 1? Name only those IDs."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('gpt-review-a') && l.includes('claude-review-a')"

  - description: "consensus-panel: explicit incompatibility makes a model unsuitable"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "A runtime catalog entry is available but explicitly dispatch-incompatible. Is it suitable for the panel? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: omitted compatibility does not make a model unsuitable"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "A runtime catalog entry is available and does not include compatibility metadata. Does the suitability rule reject it merely because that metadata is missing? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: Wave 2 falls back to explicit-high non-GPT"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Runtime discovery has no suitable GPT in its high-capability category. The first suitable high-capability non-GPT has ID claude-high. Which model ID is reserved for Wave 2? Answer with only the model ID."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('claude-high') && !l.includes('gpt-')"

  - description: "consensus-panel: Wave 2 unavailable without any high-capability model"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Runtime discovery succeeds, but no suitable high-capability model exists in any family. What is Wave 2's availability? Answer exactly one word: Available or Unavailable."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#.]/g, '').split(String.fromCharCode(96)).join(''); /^unavailable$/.test(l)"

  - description: "consensus-panel: insufficient successful catalog does not use runtime auto-selection"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Model discovery succeeded, but fewer than two distinct suitable Wave 1 IDs remain. Do I start a panel using runtime auto-selection? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: reservation never starves a two-model Wave 1"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "The successful catalog has exactly two suitable GPT IDs: runtime discovery categorizes gpt-high as high-capability and gpt-mid as mid-tier. Does the policy reserve gpt-high and leave Wave 1 unavailable? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: Wave 1 unavailability stops the review gate"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Successful discovery leaves fewer than two distinct suitable Wave 1 IDs. What must happen to the review gate and confidence?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('stop') && l.includes('review gate') && l.includes('low')"

  - description: "consensus-panel: discovery outage preserves runtime auto-selection"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Explicit model discovery is unavailable. Should both parallel Wave 1 calls omit model overrides and use runtime auto-selection? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^yes[.!]?$/.test(l)"

  - description: "consensus-panel: reports intentional reduced family diversity"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Wave 1 selected two GPT model IDs. What exact diversity note must the report include?"
    assert:
      - type: icontains
        value: "intentionally reduced model-family diversity"

  - description: "consensus-panel: no third reviewer when the initial two agree"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "This is a substantive code change, so the adaptive 2+1 panel applies and the initial wave of two reviewers has returned. They agreed on every verdict axis with high confidence and only minor findings. Does an escalation trigger fire, meaning I must dispatch the tiebreaker? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: blocker and major severities are high-risk triggers"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Which finding severities count as a high-risk escalation trigger for the panel?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('blocker') && l.includes('major')"

  - description: "consensus-panel: escalates on verdict disagreement"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "This is a substantive code change, so the adaptive 2+1 panel applies and the initial wave of two reviewers has returned. They returned different values on the correct verdict axis. Does an escalation trigger fire, meaning I must dispatch the tiebreaker? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^yes[.!]?$/.test(l)"

  - description: "consensus-panel: documents the escalation triggers"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "List the conditions that require a tiebreaker reviewer."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); const t1 = l.includes('disagree') || l.includes('differ'); const t2 = l.includes('conflict') || l.includes('contradict'); const t3 = l.includes('blocker') || l.includes('major') || l.includes('high-risk'); const t4 = l.includes('fewer than') || l.includes('insufficient') || l.includes('invalid'); const t5 = l.includes('confidence'); t1 && t2 && t3 && t4 && t5"

  - description: "consensus-panel: tiebreaker is a single high-capability model"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "When the panel escalates, how many extra reviewers do I add and what capability tier should that model be?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); const one = l.includes('one') || l.includes('single') || /(^|\\W)1(\\W|$)/.test(l); const tier = l.includes('high-capability') || l.includes('high capability'); one && tier"

  - description: "consensus-panel: docs-only change takes a single reviewer"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "The diff changes only markdown documentation files. How many reviewers do I dispatch? Answer with exactly one number."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#.]/g, '').split(String.fromCharCode(96)).join(''); /(^|\\W)(1|one)(\\W|$)/.test(l) && !/(^|\\W)(2|two|3|three)(\\W|$)/.test(l)"

  - description: "consensus-panel: one-line change takes a single reviewer"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "The diff is a one-line typo fix in a single source file, with no new control flow and no API change. How many reviewers do I dispatch? Answer with exactly one number."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#.]/g, '').split(String.fromCharCode(96)).join(''); /(^|\\W)(1|one)(\\W|$)/.test(l) && !/(^|\\W)(2|two|3|three)(\\W|$)/.test(l)"

  - description: "consensus-panel: security-sensitive tiny change is not fast-path exempt"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "The diff is one line, but it changes an authentication check. Is this scope fast-path exempt? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: fast path never uses a fast/light model"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "On the single-reviewer fast path, state the default tier, the tier for a semantically subtle tiny change, and the prohibited tier."
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); const tier = l.includes('mid-tier') || l.includes('mid tier'); const high = l.includes('high-capability') || l.includes('high capability'); const fastLight = l.includes('fast/light') || l.includes('fast-light'); const prohibited = l.includes('never') || l.includes('not use') || l.includes('avoid') || l.includes('prohibit') || l.includes('exclude') || l.includes('ineligible'); tier && high && fastLight && prohibited"

  - description: "consensus-panel: escalates when the two reviewers conflict on a finding"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "This is a substantive code change, so the adaptive 2+1 panel applies and the initial wave of two reviewers has returned. They agree on every verdict axis, but they contradict each other about the same location: one says the code is broken there and proposes the opposite fix. Does an escalation trigger fire, meaning I must dispatch the tiebreaker? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^yes[.!]?$/.test(l)"

  - description: "consensus-panel: insufficient responses is an escalation trigger"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Escalation trigger 4 is named insufficient responses. What condition does it describe?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('fewer than 2') || l.includes('fewer than two')) && l.includes('valid response')"

  - description: "consensus-panel: low synthesized confidence is an escalation trigger"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Escalation trigger 5 concerns the synthesized confidence. State the exact confidence value that fires it. Answer with one word."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#.]/g, '').split(String.fromCharCode(96)).join(''); l.includes('low') && !l.includes('high') && !l.includes('medium')"

  - description: "consensus-panel: concurrency change is not fast-path exempt"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "The diff is 4 lines in one file, but it changes locking around shared mutable state. Is this scope fast-path exempt? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: high plus medium confidence merges to medium"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Two initial reviewers agree on every verdict axis with no blocker or major finding. One reports confidence high, the other medium. Which confidence value does the synthesis record?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('medium') && !l.includes('escalat')"

  - description: "consensus-panel: dispatched single reviewer does not fan out"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "A reviewer receives an envelope with consensus_role set to single. May it select and dispatch a second reviewer of its own? Answer exactly one word: Yes or No."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#]/g, '').split(String.fromCharCode(96)).join(''); /^no[.!]?$/.test(l)"

  - description: "consensus-panel: substantive code change still takes two reviewers"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "The diff refactors 400 lines across 9 source files. How many reviewers do I dispatch in the initial wave? Answer with exactly one number."
    assert:
      - type: javascript
        value: "const l = output.trim().toLowerCase().replace(/[*_#.]/g, '').split(String.fromCharCode(96)).join(''); /(^|\\W)(2|two)(\\W|$)/.test(l) && !/(^|\\W)(3|three)(\\W|$)/.test(l)"

  - description: "consensus-panel: never duplicates research for consensus"
    vars:
      skill_content: file://../.apm/skills/consensus-panel/SKILL.md
      user_input: "Should I send three agents to search the same GitHub repository for the same facts?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); (l.includes('not') || l.includes('never') || l.includes('do not') || l.includes(\"don't\")) && (l.includes('one') || l.includes('single') || l.includes('duplica') || l.includes('identical'))"

  # --- stage-pr ---
  - description: "stage-pr: creates a preview environment"
    vars:
      skill_content: file://../.apm/skills/stage-pr/SKILL.md
      user_input: "I want to preview this PR in a running environment. How?"
    assert:
      - type: javascript
        value: "const l = output.toLowerCase(); l.includes('codespace') || l.includes('preview') || l.includes('environment') || l.includes('dev server') || l.includes('localhost')"
